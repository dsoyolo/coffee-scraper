[project]
name = "coffee-scraper-infra"
version = "1.0.0"
description = "Coffee price aggregator — AWS CDK infrastructure"
requires-python = ">=3.12"
dependencies = [
    "aws-cdk-lib==2.173.2",
    "constructs>=10.0.0,<11.0.0",
]

[tool.uv]
package = false
